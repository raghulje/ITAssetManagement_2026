# Dependencies & builds
node_modules/
dist/
dist-ssr/
build/
client/out/
.turbo/

# Secrets & local env (never commit real .env — use *.example)
.env
.env.*
!.env.example
**/.env
**/.env.*
!**/.env.example

# Host-specific compose (use docker-compose.example.yml)
docker-compose.yml
docker-compose.yaml
compose.yml
compose.yaml

# Runtime data / uploads
server/data/
**/private_uploads/
**/storage/
*.sqlite
*.sqlite-shm
*.sqlite-wal

# Production / staging SQL dumps (Wave 1.7+)
backups/

# Logs & OS
logs/
*.log
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# Binaries / installers (not source)
*.exe
*.msi
*.SED

# Local scratch / analysis
_phase0_analysis.json

# Local handoff kits (not part of ITAM product)
for_kathikaa/
for_kathikaa.zip

# Local business / PII data dumps (keep out of git)
Employee_List*.xlsx
*.xlsx
Invoice-*.pdf
Receipt-*.pdf
Sentinels_*.csv
sample-user.csv
